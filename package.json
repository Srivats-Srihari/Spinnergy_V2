{
  "name": "spinnergy-single-service",
  "version": "1.0.0",
  "private": true,
"scripts": {
  "build": "echo 'Skipping default build, use render-build instead'",
  "render-build": "npm ci --legacy-peer-deps --prefix client && npm run build --prefix client",
  "start": "node server.js",
  "postinstall": "echo 'Render-safe install complete'"
},
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "lowdb": "^6.0.1",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {}
}
